name: validate

on:
  push:
    branches: [main]
  pull_request:

jobs:
  validate:
    name: Validar tradução contra o upstream
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: validate.ps1
        shell: pwsh
        run: ./scripts/validate.ps1
